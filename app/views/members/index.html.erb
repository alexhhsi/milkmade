<h1>MilkMade members</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th>Email</th>
    <th>Username</th>
    <th>Zip code</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @members.each do |member| %>
  <tr>
    <td><%= member.first_name %></td>
    <td><%= member.last_name %></td>
    <td><%= member.email %></td>
    <td><%= member.username %></td>
    <td><%= member.zip_code %></td>
    
	<td><h1><%= member_title_links(member) %></h1></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'Sign Up', new_member_path %>
